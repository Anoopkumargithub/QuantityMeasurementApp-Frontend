{
  "name": "quantity-measurement-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "generate:config": "node ./scripts/generate-runtime-config.cjs",
    "dev": "npm run generate:config && npx @tailwindcss/cli -i ./styles/input.css -o ./dist/output.css --watch",
    "serve": "npm run generate:config && live-server",
    "build": "npm run generate:config && npx @tailwindcss/cli -i ./styles/input.css -o ./dist/output.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "dotenv": "^16.4.7",
    "tailwindcss": "^4.2.1"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
